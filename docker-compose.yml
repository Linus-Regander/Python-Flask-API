version: "3.9"

services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: python_user_api:dev
    container_name: python_user_api
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    env_file:
      - .env
    command: python main.py